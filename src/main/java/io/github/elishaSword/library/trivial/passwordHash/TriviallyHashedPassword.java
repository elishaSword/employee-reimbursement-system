package io.github.elishaSword.library.trivial.passwordHash;

import io.github.elishaSword.library.passwordHash.HashedPassword;

public class TriviallyHashedPassword extends HashedPassword {
	public TriviallyHashedPassword(String hashedPassword) {
		super(hashedPassword);
	}
}
